<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <!--
    res -> New -> Android Resource Directory -> Available Qualifiers: Night Mode ->
    arrow to the right -> Night Mode: Night;
    Project -> app -> src -> main -> res -> values-night -> copy styles.xml to the folder
    *in AndroidManifest.xml must be 'android:theme="@style/Theme.Themes"
    *in AndroidManifest.xml must not be 'android:theme="@style/MyTheme.TextSize">'
    -->
    <style name="Theme.Themes" parent="Theme.MaterialComponents">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@android:color/holo_green_dark</item>
        <item name="colorPrimaryVariant">@android:color/holo_red_dark</item>
        <item name="colorOnPrimary">@android:color/holo_orange_dark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>